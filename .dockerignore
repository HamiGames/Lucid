# Runtime data directories at root level (not needed for build)
# NOTE: blockchain/data/ is needed for Dockerfile.data builds, so we only exclude specific problematic paths
# Exclude the specific MongoDB data directory that causes permission errors
/data/mongodb/
/data/**/.mongodb
/data/**/.lock
# Exclude root-level data/ directory (runtime data, not source code)
# This won't affect blockchain/data/ since that's a different path
/data/

# Logs
logs/
**/*.log
*.log

# Cache and temporary files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg-info/
dist/
build/
.cache/
.pytest_cache/
.coverage
htmlcov/
*.egg

# Environment files
.env
.env.*
!.env.example

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Git
.git/
.gitignore
.gitattributes

# Docker files (don't need to copy Dockerfiles into the image)
Dockerfile*
docker-compose*.yml
.dockerignore

# Documentation
*.md
docs/
README*

# Tests (optional - remove if tests are needed in image)
# Exclude root-level tests/ but allow blockchain/tests/ and blockchain/scripts/ for Dockerfile builds
tests/
**/test_*.py
**/*_test.py
**/tests/
!blockchain/tests/
!blockchain/scripts/

# Node modules (if any)
node_modules/
npm-debug.log
yarn-error.log

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.swp

# Coverage reports
.coverage
htmlcov/
.pytest_cache/

# Virtual environments
venv/
env/
ENV/
.venv/

# OS files
Thumbs.db
.DS_Store

