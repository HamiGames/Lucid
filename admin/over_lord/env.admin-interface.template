# Lucid Admin Interface — bundle copy for admin-overlord snapshots
# Canonical full template: admin/env.admin-interface.template
# Keep this file in sync when adding required admin-ui keys.

ADMIN_INTERFACE_HOST=0.0.0.0
ADMIN_INTERFACE_PORT=8120
ADMIN_INTERFACE_LOG_LEVEL=INFO
ADMIN_INTERFACE_SERVICE_NAME=lucid-admin-interface

MONGODB_URI=mongodb://lucid:${MONGODB_PASSWORD}@lucid-mongodb:27017/lucid_admin?authSource=admin
REDIS_URL=redis://:${REDIS_PASSWORD}@lucid-redis:6379/5

JWT_SECRET_KEY=${JWT_SECRET_KEY}
