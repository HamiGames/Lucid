# Example overlay only — copy to configs/environment/profiles/<profile>/.env.secrets
# Keys here override the same keys from configs/environment/.env.secrets
# LUCID_CONTACT_PROFILE_KEY=<profile> or users.contact_profile_key=<profile>

# MONGODB_PASSWORD=
# REDIS_PASSWORD=
# NODE_OPERATIONAL_CONFIG_PATH=/app/config/operational-config.json
