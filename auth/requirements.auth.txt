# Authentication Service Requirements
# LUCID-STRICT Layer 1 Core Infrastructure
# Multi-platform support for Pi 5 ARM64

# Core dependencies
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
pydantic>=2.0.0
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0
passlib[bcrypt]>=1.7.4

# TRON blockchain integration (using only tronpy to avoid conflicts)
tronpy>=0.4.0
# tronapi>=3.1.6  # Commented out - conflicts with tronpy eth-abi dependency

# Database
pymongo>=4.5.0
motor>=3.3.0
redis>=5.0.0

# Hardware wallet support (minimal for now)
# ledgercomm>=1.2.2.dev7  # Commented out - may cause issues
# trezorlib>=0.13.0  # Package not available for Python 3.11
# hidapi>=0.14.0  # Package not available for Python 3.11

# HTTP client
httpx>=0.25.0
aiohttp>=3.9.0

# Monitoring
prometheus-client>=0.19.0
structlog>=23.2.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# Validation
email-validator>=2.1.0
python-multipart>=0.0.6

# Testing (for development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0