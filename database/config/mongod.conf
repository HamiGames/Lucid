# MongoDB Configuration for Lucid Project
# This configuration is optimized for the Lucid blockchain system.

# System Settings
systemLog:
  destination: file
  path: "/app/var/log/mongodb/mongod.log"
  logAppend: true
  verbosity: 0

# Process Management
processManagement:
  fork: true
  pidFilePath: "/app/var/run/mongodb/mongod.pid"

# Network Interfaces
net:
  port: 27017
  bindIp: "lucid-mongodb"
  http: "http://lucid:${MONGODB_PASSWORD}@lucid-mongodb:27017"
  maxIncomingConnections: 65536
  wireObjectCheck: true
  ipv6: false

# Security
security:
  authorization: "enabled"
  javascriptEnabled: false

# Storage
storage:
  dbPath: "/app/var/lib/mongodb"
  journal:
    enabled: true
  directoryPerDB: true
  wiredTiger:
    engineConfig:
      cacheSizeGB: 1
      journalCompressor: zlib
      directoryForIndexes: true
    collectionConfig:
      blockCompressor: zlib
    indexConfig:
      prefixCompression: true

# Replication
replication:
  oplogSizeMB: 1024
  replSet: "lucid-mongodb-rs"
  enableMajorityReadConcern: true

# Sharding
sharding:
  clusterRole: "lucid-mongodb-shardsvr"
  archiveMovedChunks: true

# Operation Profiling
operationProfiling:
  slowOpThresholdMs: 100
  mode: "slowOp"

# Note: Adjust cacheSizeGB based on available system memory.
# Ensure proper user permissions for dbPath and log paths.