name: lucid-server-external
x-repo-context:
  context: ../../../
x-lucid-build-args:
  BUILD_DATE: ${BUILD_DATE:-}
  VCS_REF: ${VCS_REF:-}
  VERSION: ${VERSION:-1.0.0}
  TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
  BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
  PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
x-env-server: &id001
- ../../../configs/environment/.env.master
- ../../environment/.env.secrets
services:
  server-gateway:
    image: ${LUCID_SERVER_GATEWAY_IMAGE:-pickme/lucid-server-gateway:latest-arm64}
    container_name: lucid-server-gateway
    hostname: api-gateway
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/server/Dockerfile.server-gateway
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      LUCID_ENV: ${LUCID_ENV:-production}
      WALLET_MANAGER_URL: ${WALLET_MANAGER_URL:-http://wallet-manager:8093}
      DATA_CHAIN_URL: ${DATA_CHAIN_URL:-http://data-chain:8087}
      TRON_CLIENT_URL: ${TRON_CLIENT_URL:-http://tron-client:8091}
      SESSION_API_URL: ${SESSION_API_URL:-http://session-api:8113}
      NODE_SYSTEM_GATEWAY_URL: ${NODE_SYSTEM_GATEWAY_URL:-http://node-system-gateway:8640}
      ADMIN_SYSTEM_GATEWAY_URL: ${ADMIN_SYSTEM_GATEWAY_URL:-http://admin-system-gateway:8155}
    ports:
    - ${SERVER_GATEWAY_PUBLISH_PORT:-8080}:8080
    networks:
      lucid-pi-network:
        aliases:
        - server-gateway
      lucid-server-network:
        aliases:
        - server-gateway
      lucid-session-network:
        aliases:
        - server-gateway
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=200m
    security_opt:
    - no-new-privileges:true
    cap_drop:
    - ALL
    cap_add:
    - NET_BIND_SERVICE
    labels:
      com.lucid.service: server-gateway
      com.lucid.expose: '8080'
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
  lucid-server-manager:
    image: ${LUCID_SERVER_MANAGER_IMAGE:-pickme/lucid-server-manager:latest-arm64}
    container_name: lucid-server-manager
    hostname: lucid-server-manager
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/server/Dockerfile.server-manager
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      LUCID_ENV: ${LUCID_ENV:-production}
    ports:
    - ${LUCID_SERVER_MANAGER_PUBLISH_PORT:-8081}:8081
    networks:
    - lucid-pi-network
    - lucid-server-network
    - lucid-session-network
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=200m
    security_opt:
    - no-new-privileges:true
    labels:
      com.lucid.service: lucid-server-manager
      com.lucid.expose: '8081'
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
  lucid-server-core:
    image: ${LUCID_SERVER_CORE_IMAGE:-pickme/lucid-server-core:latest-arm64}
    container_name: lucid-server-core
    hostname: lucid-server-core
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/server/Dockerfile
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      LUCID_ENV: ${LUCID_ENV:-production}
      LUCID_SERVER_CORE_PORT: '8100'
    ports:
    - ${LUCID_SERVER_CORE_PUBLISH_PORT:-8100}:8100
    networks:
    - lucid-pi-network
    - lucid-server-network
    - lucid-session-network
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=200m
    security_opt:
    - no-new-privileges:true
    labels:
      com.lucid.service: lucid-server-core
      com.lucid.expose: '8100'
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
networks:
  lucid-pi-network:
    external: true
    name: lucid-pi-network
  lucid-server-network:
    external: true
    name: lucid-server-network
  lucid-session-network:
    external: true
    name: lucid-session-network
secrets:
  MONGODB_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/MONGODB_PASSWORD.secret
  REDIS_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/REDIS_PASSWORD.secret
  JWT_SECRET_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/JWT_SECRET_KEY.secret
  ENCRYPTION_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/ENCRYPTION_KEY.secret
  SESSION_SECRET:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/SESSION_SECRET.secret
  TRON_PRIVATE_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TRON_PRIVATE_KEY.secret
  TRON_API_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TRON_API_KEY.secret
  API_GATEWAY_SECRET:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/API_GATEWAY_SECRET.secret
  ELASTICSEARCH_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/ELASTICSEARCH_PASSWORD.secret
  TOR_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TOR_PASSWORD.secret
  TOR_CONTROL_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TOR_CONTROL_PASSWORD.secret
  WALLET_ENCRYPTION_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/WALLET_ENCRYPTION_KEY.secret
