name: lucid-node-data-system
x-repo-context:
  context: ../../../
x-lucid-build-args:
  BUILD_DATE: ${BUILD_DATE:-}
  VCS_REF: ${VCS_REF:-}
  VERSION: ${VERSION:-1.0.0}
  TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
  BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
  PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
x-env-node-data: &id001
- ../../../configs/environment/.env.master
- ../../environment/.env.secrets
services:
  node-worker-data:
    image: ${LUCID_NODE_WORKER_IMAGE:-pickme/lucid-node-worker:latest-arm64}
    container_name: lucid-node-data-node-worker
    hostname: node-worker-data
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/node/Dockerfile.node-worker
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      NODE_MANAGEMENT_HOST: 0.0.0.0
      NODE_MANAGEMENT_PORT: '8600'
      NODE_MANAGEMENT_URL: http://node-worker-data:8600
      DATA_CHAIN_URL: ${DATA_CHAIN_URL:-http://data-chain:8087}
      BLOCK_MANAGER_URL: ${BLOCK_MANAGER_URL:-http://block-manager:8086}
      SESSION_API_URL: ${SESSION_API_URL:-http://session-api:8113}
      BLOCKCHAIN_ENGINE_URL: ${BLOCKCHAIN_ENGINE_URL:-http://blockchain-engine:8084}
      MONGODB_URL: ${NODE_MONGODB_URL:-mongodb://lucid-mongodb:27017/lucid}
      REDIS_URL: ${NODE_REDIS_URL:-redis://lucid-redis:6379/0}
    ports:
    - ${NODE_DATA_NODE_WORKER_PUBLISH_PORT:-18600}:8600
    networks:
    - lucid-pi-network
    - lucid-session-network
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=100m
    security_opt:
    - no-new-privileges:true
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
  data-chain:
    image: ${LUCID_DATA_CHAIN_IMAGE:-pickme/lucid-data-chain:latest-arm64}
    container_name: data-chain
    hostname: data-chain
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/blockchain/Dockerfile.data
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      DATA_CHAIN_PORT: '8087'
      BLOCKCHAIN_ENGINE_URL: ${BLOCKCHAIN_ENGINE_URL:-http://blockchain-engine:8084}
      BLOCK_MANAGER_URL: ${BLOCK_MANAGER_URL:-http://block-manager:8086}
      SESSION_API_URL: ${SESSION_API_URL:-http://session-api:8113}
      MONGODB_URL: ${MONGODB_URL:-mongodb://lucid-mongodb:27017/lucid}
      REDIS_URL: ${REDIS_URL:-redis://lucid-redis:6379/0}
    ports:
    - ${DATA_CHAIN_PUBLISH_PORT:-8087}:8087
    networks:
    - lucid-pi-network
    - lucid-session-network
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=200m
    security_opt:
    - no-new-privileges:true
    labels:
      com.lucid.service: data-chain
      com.lucid.expose: '8087'
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
  block-manager:
    image: ${LUCID_BLOCK_MANAGER_IMAGE:-pickme/lucid-block-manager:latest-arm64}
    container_name: block-manager
    hostname: block-manager
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/blockchain/Dockerfile.block-manager
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      BLOCK_MANAGER_PORT: '8086'
      BLOCKCHAIN_ENGINE_URL: ${BLOCKCHAIN_ENGINE_URL:-http://blockchain-engine:8084}
      DATA_CHAIN_URL: ${DATA_CHAIN_URL:-http://data-chain:8087}
      SESSION_API_URL: ${SESSION_API_URL:-http://session-api:8113}
      MONGODB_URL: ${MONGODB_URL:-mongodb://lucid-mongodb:27017/lucid}
      REDIS_URL: ${REDIS_URL:-redis://lucid-redis:6379/0}
    ports:
    - ${BLOCK_MANAGER_PUBLISH_PORT:-8086}:8086
    networks:
    - lucid-pi-network
    - lucid-session-network
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=200m
    security_opt:
    - no-new-privileges:true
    labels:
      com.lucid.service: block-manager
      com.lucid.expose: '8086'
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
  chain-to-pay:
    image: ${LUCID_CHAIN_TO_PAY_IMAGE:-pickme/lucid-chain-to-pay:latest-arm64}
    container_name: chain-to-pay
    hostname: chain-to-pay
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    build:
      context: ../../../
      dockerfile: infrastructure/containers/blockchain/Dockerfile.chain-to-pay
      args:
        BUILD_DATE: ${BUILD_DATE:-}
        VCS_REF: ${VCS_REF:-}
        VERSION: ${VERSION:-1.0.0}
        TARGETPLATFORM: ${TARGETPLATFORM:-linux/arm64}
        BUILDPLATFORM: ${BUILDPLATFORM:-linux/amd64}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.11}
    restart: unless-stopped
    env_file: *id001
    environment:
      CHAIN_TO_PAY_PORT: '8114'
      TRON_PAYMENT_SERVICE_URL: ${TRON_PAYMENT_SERVICE_URL:-http://tron-payment-service:8080}
      DATA_CHAIN_URL: ${DATA_CHAIN_URL:-http://data-chain:8087}
      BLOCKCHAIN_ENGINE_URL: ${BLOCKCHAIN_ENGINE_URL:-http://blockchain-engine:8084}
      SESSION_CHUNK_PROCESSOR_URL: ${SESSION_CHUNK_URL:-http://session-chunk-processor:8010}
    ports:
    - ${CHAIN_TO_PAY_PUBLISH_PORT:-8114}:8114
    networks:
    - lucid-pi-network
    - lucid-session-network
    - lucid-tron-isolated
    user: 65532:65532
    read_only: true
    tmpfs:
    - /tmp:noexec,nosuid,size=200m
    security_opt:
    - no-new-privileges:true
    labels:
      com.lucid.service: chain-to-pay
      com.lucid.expose: '8114'
    volumes:
    - ../../environment/.env.master:/app/configs/.env.master:ro
    - ../../environment/.env.secrets:/app/configs/.env.secrets:ro
networks:
  lucid-pi-network:
    external: true
    name: lucid-pi-network
  lucid-session-network:
    external: true
    name: lucid-session-network
  lucid-tron-isolated:
    external: true
    name: lucid-tron-isolated
secrets:
  MONGODB_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/MONGODB_PASSWORD.secret
  REDIS_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/REDIS_PASSWORD.secret
  JWT_SECRET_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/JWT_SECRET_KEY.secret
  ENCRYPTION_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/ENCRYPTION_KEY.secret
  SESSION_SECRET:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/SESSION_SECRET.secret
  TRON_PRIVATE_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TRON_PRIVATE_KEY.secret
  TRON_API_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TRON_API_KEY.secret
  API_GATEWAY_SECRET:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/API_GATEWAY_SECRET.secret
  ELASTICSEARCH_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/ELASTICSEARCH_PASSWORD.secret
  TOR_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TOR_PASSWORD.secret
  TOR_CONTROL_PASSWORD:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/TOR_CONTROL_PASSWORD.secret
  WALLET_ENCRYPTION_KEY:
    file: C:/Users/surba/Desktop/personal/THE_FUCKER/lucid_2/Lucid/configs/secrets/runtime/compose/WALLET_ENCRYPTION_KEY.secret
