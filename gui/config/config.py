""" Configuration for Lucid GUI services 
file: /app/gui/config/config.py
x-lucid-file-path: /app/gui/config/config.py
x-lucid-file-type: python

about this script:
- hosts the core Classes for the GUI services
- includes the config classes for the GUI services
- calls the server-config for the GUI services
- calls the database-config for the GUI services
- calls the blockchain-config for the GUI services
- calls the api-gateway-config for the GUI services
- calls the auth-config for the GUI services
- calls the admin-config for the GUI services
- calls the user-config for the GUI services
- calls the node-config for the GUI services
- calls the wallet-config for the GUI services
- calls the electron-config for the GUI services

"""

#load imports

# create GuiConfig class


# create ServerConfig class

# create GuiDatabaseConfig class


# create GuiLedgerConfig class (the only gui-blockchain connection)


# create GuiApiGatewayConfig class


# create GuiAuthConfig class

# create GuiAdminConfig class

# create GuiUserConfig class

# create GuiNodeConfig class


# create GuiWalletConfig class


# create GuiElectronConfig class


# link to gui/main.py for triggering fast-api connections to other services