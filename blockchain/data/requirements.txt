# Data Chain Service Dependencies
# Minimal requirements for data-chain container only

# Core framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
pydantic>=2.5.0

# Database (MongoDB only - no PostgreSQL)
motor>=3.3.0
pymongo>=4.6.0

# Hashing & Integrity
blake3>=0.3.0

# Configuration
pyyaml>=6.0.0

# Compression (required for storage operations - used in storage.py)
lz4>=4.3.0
zstandard>=0.22.0

# HTTP client (required for blockchain-engine communication)
httpx>=0.25.0
aiohttp>=3.9.0
