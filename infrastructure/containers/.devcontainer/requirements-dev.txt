# Lucid RDP Development Environment Requirements
# Based on pyproject.toml and container-specific needs

# === Core API Framework ===
fastapi>=0.111,<1.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
httpx>=0.25.0
pydantic>=2.5.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
PyJWT>=2.8.0

# === CRITICAL MISSING MODULES (per user rules) ===
# These modules MUST be installed to avoid ModuleNotFoundError
base58>=2.1.1
blake3>=0.3.3
msgpack>=1.0.0
tronpy>=0.4.0
dnspython>=2.4.0
email-validator>=2.1.0
python-dateutil>=2.8.2
PyYAML>=6.0.1

# === Database (MongoDB 7 only per R-MUST-019) ===
motor>=3.3.0
pymongo>=4.6.0
pytest-asyncio>=0.23.0

# === Blockchain & Networking ===
requests>=2.31.0
cryptography>=41.0.0
websockets>=12.0
netmiko>=4.3.0

# === Cryptography (per Spec-1b requirements) ===
# XChaCha20-Poly1305 and BLAKE3 for session encryption
PyNaCl>=1.5.0
blake3>=0.3.3

# === TRON Integration (per Spec-1b) ===
# NOTE: tronpy already added above in CRITICAL MISSING MODULES

# === Testing Framework ===
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx[cli]>=0.25.0
asgi-lifespan>=2.1.0
openapi-spec-validator>=0.7.0

# === Code Quality & Linting ===
black>=23.12.0
ruff>=0.1.8
mypy>=1.8.0
bandit[toml]>=1.7.5
pre-commit>=3.6.0

# === Type Stubs ===
types-requests>=2.31.0
types-PyYAML>=6.0.12
types-python-dateutil>=2.8.19

# === CLI & Utilities ===
click>=8.1.0

# === Container-specific Dependencies ===
# Process management and system tools
supervisor>=4.2.0

# === Compression (Zstd for chunks per Spec-1b) ===
zstandard>=0.22.0

# === Development Utilities ===
# Better debugging and development experience
ipython>=8.18.0
jupyter>=1.0.0
rich>=13.7.0

# === Security & Monitoring ===
# Additional security scanning
safety>=2.3.0

# === Networking & Tor Integration ===
# SOCKS proxy support for Tor integration
PySocks>=1.7.1
stem>=1.8.2

# === Data Processing ===
# JSON handling optimized for performance
orjson>=3.9.0

# === Documentation ===
# API documentation generation
fastapi-utils>=0.2.1

# === Performance Monitoring ===
# Optional performance profiling
line_profiler>=4.1.0
