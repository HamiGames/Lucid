# syntax=docker/dockerfile:1.5
# File: infrastructure/containers/database/Dockerfile.elasticsearch
#
# LUCID Elasticsearch - Distroless Java 17 + Elasticsearch 8.x
# Design aligned with 03_api_gateway/Dockerfile (syntax, ARGs, stage headers, labels, verification, health timing, USER 65532, tini)
# Builder: official Elasticsearch image (correct base for ES binaries, plugins, and keystore creation)
# Runtime: gcr.io/distroless/java17-debian12 (not Python distroless — correct for Elasticsearch)
# Build context: repository root

ARG BUILD_DATE=
ARG VCS_REF=
ARG VERSION=1.0.0
ARG TARGETPLATFORM=linux/arm64
ARG BUILDPLATFORM=linux/amd64

############################
# Stage 1 – Elasticsearch builder
############################
# Use global ARG TARGETPLATFORM (lines above); bare ARG TARGETPLATFORM before FROM can be empty.
FROM --platform=$TARGETPLATFORM elasticsearch:8.11.0 AS es-builder

WORKDIR /build



RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
    --mount=type=cache,target=/var/lib/apt,sharing=locked \
    rm -rf /var/lib/apt/lists/* && \
    apt-get update --allow-releaseinfo-change && \
    apt-get install -y --no-install-recommends \
        bash \
        ca-certificates \
        coreutils \
        curl \
        tini \
        build-essential \
        gcc \
        g++ \
        libffi-dev \
        libssl-dev \
        libpq-dev \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        python3.11 \
        python3.11-pip \
        python3.11-setuptools \
        python3.11-wheel \
        tini \
        xxd \
        openssl \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
# LUCID_X_FILES_SKELETON_BEGIN
# Dirs: (listing ∩ COPY-approved) ∪ builder-stage COPY roots; after pip cache RUN or before first /build COPY
RUN set -eux; \
    mkdir -p '.' './common' './configs' './service_mesh' './common/__pycache__' './common/governance' './common/security' './common/server-tools' './common/templates' './common/tor' './configs/.ssh' './configs/compose' './configs/container' './configs/database' './configs/docker' './configs/env' './configs/environment' './configs/integration' './configs/kubernetes' './configs/logging' './configs/pi' './configs/secrets' './configs/security' './configs/tor' './service_mesh/__pycache__' './service_mesh/communication' './service_mesh/config' './service_mesh/controller' './service_mesh/discovery' './service_mesh/docs' './service_mesh/security' './service_mesh/sidecar' './common/governance/__pycache__' './common/security/__pycache__' './common/server-tools/__pycache__' './common/tor/__pycache__' './configs/container/admin' './configs/container/auth' './configs/container/base' './configs/container/blockchain' './configs/container/database' './configs/container/electron_gui' './configs/container/gui' './configs/container/node' './configs/container/payment_systems' './configs/container/rdp' './configs/container/server' './configs/container/sessions' './configs/container/storage' './configs/container/tor' './configs/database/elasticsearch' './configs/database/redis' './configs/docker/distroless' './configs/docker/docs' './configs/docker/multi-stage' './configs/pi/ssh' './service_mesh/communication/__pycache__' './service_mesh/config/policies' './service_mesh/config/services' './service_mesh/controller/__pycache__' './service_mesh/discovery/__pycache__' './service_mesh/security/__pycache__' './service_mesh/sidecar/envoy' './service_mesh/sidecar/proxy' './service_mesh/sidecar/envoy/__pycache__' './service_mesh/sidecar/envoy/config' './service_mesh/sidecar/proxy/__pycache__'; \
    for d in \
      '.' \
      './common' \
      './configs' \
      './service_mesh' \
      './common/__pycache__' \
      './common/governance' \
      './common/security' \
      './common/server-tools' \
      './common/templates' \
      './common/tor' \
      './configs/.ssh' \
      './configs/compose' \
      './configs/container' \
      './configs/database' \
      './configs/docker' \
      './configs/env' \
      './configs/environment' \
      './configs/integration' \
      './configs/kubernetes' \
      './configs/logging' \
      './configs/pi' \
      './configs/secrets' \
      './configs/security' \
      './configs/tor' \
      './service_mesh/__pycache__' \
      './service_mesh/communication' \
      './service_mesh/config' \
      './service_mesh/controller' \
      './service_mesh/discovery' \
      './service_mesh/docs' \
      './service_mesh/security' \
      './service_mesh/sidecar' \
      './common/governance/__pycache__' \
      './common/security/__pycache__' \
      './common/server-tools/__pycache__' \
      './common/tor/__pycache__' \
      './configs/container/admin' \
      './configs/container/auth' \
      './configs/container/base' \
      './configs/container/blockchain' \
      './configs/container/database' \
      './configs/container/electron_gui' \
      './configs/container/gui' \
      './configs/container/node' \
      './configs/container/payment_systems' \
      './configs/container/rdp' \
      './configs/container/server' \
      './configs/container/sessions' \
      './configs/container/storage' \
      './configs/container/tor' \
      './configs/database/elasticsearch' \
      './configs/database/redis' \
      './configs/docker/distroless' \
      './configs/docker/docs' \
      './configs/docker/multi-stage' \
      './configs/pi/ssh' \
      './service_mesh/communication/__pycache__' \
      './service_mesh/config/policies' \
      './service_mesh/config/services' \
      './service_mesh/controller/__pycache__' \
      './service_mesh/discovery/__pycache__' \
      './service_mesh/security/__pycache__' \
      './service_mesh/sidecar/envoy' \
      './service_mesh/sidecar/proxy' \
      './service_mesh/sidecar/envoy/__pycache__' \
      './service_mesh/sidecar/envoy/config' \
      './service_mesh/sidecar/proxy/__pycache__' \
    ; do \
      printf 'LUCID_X_FILES_SKELETON_%s' "$(date +%s)" > "$d/.keep"; \
      touch "$d/.keep"; \
    done
# LUCID_X_FILES_SKELETON_END
# LUCID_ALIGNMENT_MAT_COPY_DIRECTORIES_BEGIN
# alignment-mat elasticsearch-storage_manifest.json: compose_service='elasticsearch-storage' — explicit manifest paths → 23 builder COPY line(s) (dockerfile_layout_structure.json section #10 COPY_DIRECTORIES).
COPY common/__init__.py ./common/__init__.py
COPY common/contact_profile_env.py ./common/contact_profile_env.py
COPY common/governance/__init__.py ./common/governance/__init__.py
COPY common/governance/lucid_governor.py ./common/governance/lucid_governor.py
COPY common/governance/param_registry.py ./common/governance/param_registry.py
COPY common/governance/timelock.py ./common/governance/timelock.py
COPY common/load_host_config.py ./common/load_host_config.py
COPY common/security/__init__.py ./common/security/__init__.py
COPY common/security/input_controller.py ./common/security/input_controller.py
COPY common/security/policy_validator.py ./common/security/policy_validator.py
COPY common/security/privacy_shield.py ./common/security/privacy_shield.py
COPY common/security/session_validator.py ./common/security/session_validator.py
COPY common/security/trust_nothing_engine.py ./common/security/trust_nothing_engine.py
COPY common/server-tools/__init__.py ./common/server-tools/__init__.py
COPY common/tor/__init__.py ./common/tor/__init__.py
COPY common/tor/onion_service.py ./common/tor/onion_service.py
COPY common/tor/socks_proxy.py ./common/tor/socks_proxy.py
COPY common/tor/tor_manager.py ./common/tor/tor_manager.py
COPY infrastructure/containers/config-listings.json ./infrastructure/containers/config-listings.json
COPY infrastructure/containers/host-config.yml ./infrastructure/containers/host-config.yml
COPY infrastructure/containers/services/middle-ware/master-endpoint.yml ./infrastructure/containers/services/middle-ware/master-endpoint.yml
COPY infrastructure/containers/services/storage/elasticsearch-storage.yml ./infrastructure/containers/services/storage/elasticsearch-storage.yml
COPY infrastructure/containers/services/x_lucid_cluster_calibration/allocation_manifest.yml ./infrastructure/containers/services/x_lucid_cluster_calibration/allocation_manifest.yml
# LUCID_ALIGNMENT_MAT_COPY_DIRECTORIES_END
# LUCID_LIB_SKELETON_BEGIN
# Generated: FHS dirs for builder (from final-stage COPY --from, excluding /build/ sources;
# ancestor dirs collapsed; see collect_skeleton_paths_from_dockerfile).
COPY infrastructure/compose/ ./configs/compose/





COPY infrastructure/service_mesh/ ./service_mesh/
COPY infrastructure/docker/ ./infrastructure/docker/



RUN rm -rf configs/environment 2>/dev/null || true


RUN printf "LUCID_ELASTICSEARCH_RUNTIME_DIRECTORY" > /var/run/.keep && \
    printf "LUCID_ELASTICSEARCH_LIB_DIRECTORY" > /var/lib/.keep && \
    chmod 644 /var/run/.keep /var/lib/.keep

RUN cat > /usr/share/elasticsearch/config/elasticsearch.yml <<'EOFCONFIG'
cluster.name: lucid-cluster
node.name: lucid-node
discovery.type: single-node
xpack.security.enabled: false
xpack.security.enrollment.enabled: false
xpack.security.http.ssl.enabled: false
xpack.security.transport.ssl.enabled: false
bootstrap.memory_lock: false
network.host: lucid-pi-network
http.port: 9200
transport.port: 9300
EOFCONFIG

RUN /usr/share/elasticsearch/bin/elasticsearch-keystore create && \
    chown elasticsearch:elasticsearch /usr/share/elasticsearch/config/elasticsearch.keystore && \
    echo "✅ Elasticsearch keystore created during build"

RUN chmod 644 /usr/share/elasticsearch/config/elasticsearch.yml
RUN chown elasticsearch:elasticsearch /usr/share/elasticsearch/config/elasticsearch.yml
RUN echo "✅ Elasticsearch config copied and permissions set"

RUN test -f /usr/share/elasticsearch/bin/elasticsearch && \
    test -f /usr/share/elasticsearch/config/elasticsearch.yml && \
    test -f /usr/share/elasticsearch/config/elasticsearch.keystore && \
    test -f /etc/ssl/certs/ca-certificates.crt && \
    echo "✅ Elasticsearch builder artifacts verified"

# Export glibc + arch libs for distroless root overlay (multi-arch via dpkg-architecture)
RUN MULTIARCH=$(dpkg-architecture -qDEB_HOST_MULTIARCH) && \
    mkdir -p "/export/rootfs/lib/${MULTIARCH}" /export/rootfs/lib64 && \
    cp -a "/lib/${MULTIARCH}/." "/export/rootfs/lib/${MULTIARCH}/" && \
    if [ -f /lib/ld-linux-aarch64.so.1 ]; then cp /lib/ld-linux-aarch64.so.1 /export/rootfs/lib/; fi && \
    if [ -f /lib64/ld-linux-x86-64.so.2 ]; then cp /lib64/ld-linux-x86-64.so.2 /export/rootfs/lib64/; fi && \
    echo "✅ Exported dynamic linker layout for ${MULTIARCH}"

############################
# Stage 2 – Runtime (Distroless Java 17)
############################
FROM --platform=$TARGETPLATFORM gcr.io/distroless/java17-debian12:latest

ARG BUILD_DATE=
ARG VCS_REF=
ARG VERSION=1.0.0
# Lucid Tor egress / network isolation (PUBLIC_TOR_IP: ../../configs/environment/ ; compose: lucid-tor-egress-network)
ARG PUBLIC_TOR_IP=109.70.100.1
ARG LUCID_INTERNAL_NETWORK_NAME=lucid-pi-network
ARG LUCID_TOR_EGRESS_NETWORK_NAME=lucid-tor-egress-network

WORKDIR /app

LABEL maintainer="Lucid Development Team" \
      com.lucid.network.internal="${LUCID_INTERNAL_NETWORK_NAME}" \
      com.lucid.network.tor_egress="${LUCID_TOR_EGRESS_NETWORK_NAME}" \
      com.lucid.tor.public_egress_ip="${PUBLIC_TOR_IP}" \
      org.opencontainers.image.title="Lucid Elasticsearch" \
      org.opencontainers.image.description="Distroless Elasticsearch search engine for Lucid" \
      org.opencontainers.image.version="${VERSION}" \
      org.opencontainers.image.revision="${VCS_REF}" \
      org.opencontainers.image.created="${BUILD_DATE}" \
      com.lucid.service="lucid-elasticsearch" \
      com.lucid.platform="arm64" \
      com.lucid.security="distroless" \
      com.lucid.cluster="foundation" \
      com.lucid.elasticsearch.version="8.11.0"

ENV     PUBLIC_TOR_IP="${PUBLIC_TOR_IP}" \
    LUCID_INTERNAL_NETWORK_NAME="${LUCID_INTERNAL_NETWORK_NAME}" \
    LUCID_TOR_EGRESS_NETWORK_NAME="${LUCID_TOR_EGRESS_NETWORK_NAME}" \
    ES_HOME=/app/usr/share/elasticsearch \
    PATH=/app/usr/share/elasticsearch/bin:/app/usr/bin:/app/bin \
    CLUSTER_NAME=lucid-cluster \
    NODE_NAME=lucid-node \
    DISCOVERY_TYPE=single-node \
    XPACK_SECURITY_ENABLED=false \
    LUCID_HOST_CONFIG_SERVICE_NAME=lucid_elasticsearch_http \
    LUCID_SERVICE_HOST_IP=172.20.10.68 \
    LUCID_SERVICE_HTTP_PATH=http://lucid-elasticsearch-http:9200/app \
    LUCID_ELASTICSEARCH_TRANSPORT_HOST_IP=172.20.10.69 \
    LUCID_ELASTICSEARCH_TRANSPORT_SERVICE_PATH=http://lucid-elasticsearch-transport:9300/app
# LUCID_RUNTIME_COPY_FROM_BUILD_BEGIN
# Generated: same directory set as LUCID_X_FILES_SKELETON (./ under WORKDIR /build).
COPY --from=es-builder --chown=65532:65532 /build/common/ /app/common/
COPY --from=es-builder --chown=65532:65532 /build/configs/ /app/configs/
COPY --from=es-builder --chown=65532:65532 /build/service_mesh/ /app/service_mesh/
# LUCID_RUNTIME_COPY_FROM_BUILD_END

# LUCID_RUNTIME_COPY_FROM_BUILDER_APT_BEGIN
# Generated: apt + RUN scaffold (mkdir/printf/touch/chown) -> FHS (not ./ or /build/; lib_search_and_inject.py).
COPY --from=es-builder --chown=root /usr/lib/*/libcurl.so* /app/usr/lib/
COPY --from=es-builder --chown=root /usr/lib/*/libffi.so* /app/usr/lib/
COPY --from=es-builder --chown=root /usr/lib/*/libssl.so* /app/usr/lib/
COPY --from=es-builder --chown=root /usr/lib/*/ossl-modules/ /app/usr/lib/
COPY --from=es-builder --chown=root /usr/lib/*/libpq.so.5 /app/usr/lib/
COPY --from=es-builder --chown=root /bin/ /app/bin/
COPY --from=es-builder --chown=root /dev/null/ /app/dev/null/
COPY --from=es-builder --chown=root /etc/ssl/ /app/etc/ssl/
COPY --from=es-builder --chown=root /export/rootfs/lib64/ /app/export/rootfs/lib64/
COPY --from=es-builder --chown=root /usr/bin/ /app/usr/bin/
COPY --from=es-builder --chown=root /usr/lib/python3.11/ /app/usr/lib/python3.11/
COPY --from=es-builder --chown=root /usr/share/elasticsearch/config/ /app/usr/share/elasticsearch/config/
COPY --from=es-builder --chown=root /var/lib/ /app/var/lib/
COPY --from=es-builder --chown=root /var/run/ /app/var/run/
# LUCID_RUNTIME_COPY_FROM_BUILDER_APT_END








# Generated: apt + RUN scaffold (mkdir/printf/touch/chown) -> FHS (not ./ or /build/; lib_search_and_inject.py).

RUN test -d /app/usr/share/elasticsearch || { echo "CRITICAL: elasticsearch directory missing"; exit 1; } && \
    test -f /app/usr/share/elasticsearch/bin/elasticsearch || { echo "CRITICAL: elasticsearch binary missing"; exit 1; } && \
    test -f /app/usr/share/elasticsearch/config/elasticsearch.yml || { echo "CRITICAL: elasticsearch config missing"; exit 1; } && \
    test -f /app/usr/share/elasticsearch/config/elasticsearch.keystore || { echo "CRITICAL: keystore missing"; exit 1; } && \
    test -f /app/usr/bin/bash || { echo "CRITICAL: bash missing"; exit 1; } && \
    test -f /app/usr/bin/tini || { echo "CRITICAL: tini missing"; exit 1; } && \
    test -f /app/usr/bin/curl || { echo "CRITICAL: curl missing"; exit 1; } && \
    test -f /app/etc/ssl/certs/ca-certificates.crt || { echo "CRITICAL: CA bundle missing"; exit 1; } && \
    echo "✅ Elasticsearch runtime verification passed"

EXPOSE 9200 9300

HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD ["/app/usr/bin/curl", "-f", "http://127.0.0.1:9200/_cluster/health"]

USER 65532:65532

# ES_JAVA_OPTS from compose (e.g. heap) must apply; configs/database/elasticsearch/elasticsearch.yml documents compose env for JVM.
USER root

USER elasticsearch
USER root

ENTRYPOINT ["/app/usr/bin/tini", "--", "/app/usr/bin/bash", "-c", "exec /app/usr/share/elasticsearch/bin/elasticsearch"]
CMD []
SHELL ["/app/usr/bin/bash", "-c"]
