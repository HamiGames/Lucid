# =============================================================================
# GUI Docker Manager Service - Environment Variables Template
# =============================================================================
# File: gui-docker-manager/gui-docker-manager/config/env.gui-docker-manager.template
# Purpose: Template for all configurable environment variables
# Usage: Copy to configs/environment/.env.gui-docker-manager and customize values
# =============================================================================

# =============================================================================
# SERVICE CONFIGURATION
# =============================================================================
SERVICE_NAME=gui-docker-manager
SERVICE_VERSION=1.0.0
PORT=8098
HOST=0.0.0.0
SERVICE_URL=http://lucid-gui-docker-manager:8098

# Platform and Environment
LUCID_ENV=production
LUCID_PLATFORM=arm64
DEBUG=false
LOG_LEVEL=INFO

# =============================================================================
# DOCKER CONFIGURATION (REQUIRED)
# =============================================================================
# Docker socket path - REQUIRED
DOCKER_HOST=unix:///var/run/docker.sock

# Docker API version
DOCKER_API_VERSION=1.40

# =============================================================================
# DATABASE CONFIGURATION (OPTIONAL)
# =============================================================================
# MongoDB connection URL - Optional for metrics storage
MONGODB_URL=mongodb://lucid:{MONGODB_PASSWORD}@lucid-mongodb:27017/lucid?authSource=admin

# Redis connection URL - Optional for caching
REDIS_URL=redis://:${REDIS_PASSWORD}@lucid-redis:6379/0

# =============================================================================
# SECURITY CONFIGURATION (REQUIRED)
# =============================================================================
# JWT secret key - REQUIRED for authentication
JWT_SECRET_KEY={JWT_SECRET_KEY}

# =============================================================================
# ACCESS CONTROL CONFIGURATION
# =============================================================================
# Enable role-based access control
GUI_ACCESS_LEVELS_ENABLED=true

# User service access levels (comma-separated)
USER_SERVICES=foundation,core

# Developer service access levels (comma-separated)
DEVELOPER_SERVICES=foundation,core,application

# Admin service access levels (comma-separated)
ADMIN_SERVICES=foundation,core,application,support

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=http://localhost:*,https://localhost:*,file://*

# =============================================================================
# DOCKER COMPOSE MANAGEMENT
# =============================================================================
# Docker Compose files directory
DOCKER_COMPOSE_FILES_DIR=/mnt/myssd/Lucid/Lucid/configs/docker/

# Default compose files to manage
DOCKER_COMPOSE_FILES=docker-compose.gui-integration.yml,docker-compose.application.yml,docker-compose.core.yml,docker-compose.foundation.yml

# =============================================================================
# HEALTH CHECK
# =============================================================================
# Health check interval (seconds)
HEALTH_CHECK_INTERVAL=30

# Health check timeout (seconds)
HEALTH_CHECK_TIMEOUT=10

# =============================================================================
# SERVICE COMMUNICATION (SERVICE-TO-SERVICE)
# =============================================================================
# Service request timeout (seconds)
SERVICE_TIMEOUT_SECONDS=30

# Service retry count
SERVICE_RETRY_COUNT=3

# Service retry delay (seconds)
SERVICE_RETRY_DELAY_SECONDS=1.0

# =============================================================================
# PROJECT CONFIGURATION
# =============================================================================
# Project root directory
PROJECT_ROOT=/mnt/myssd/Lucid/Lucid
